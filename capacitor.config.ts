import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pesatto.ums',
  appName: 'PESATTO UMS',
  webDir: 'dist/pesatto-gms-front/browser'
};

export default config;
